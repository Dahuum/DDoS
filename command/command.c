/* command.c */
#include "command.h"
#include <stdio.h>

int main(int ac, char *av[]) {
    // init();
    
    load((int8)1, 'X');
    printf("\n\n");
    
    dinit();
    
    // p = strnum("drive.", 1);
    // printf("strnum: %s\n", p);
    
    return 0;
}